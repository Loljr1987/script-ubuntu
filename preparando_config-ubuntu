# Atualize o sistema e instale o Git, caso necessário
sudo apt update && sudo apt install -y git

# Clone o repositório com o script
git clone https://github.com/Loljr1987/script-ubuntu.git

# Entre na pasta do repositório
cd script-ubuntu

# Torne o script executável
chmod +x config-ubuntu.sh

# Execute o script
./config-ubuntu.sh

